name: "Post Build"

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  post-build:
    runs-on: ubuntu-latest
    steps:
      - name: info
        run: |
          env
          echo "${{ toJSON(github.event.workflow_run) }}"
