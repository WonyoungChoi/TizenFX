name: "Postback Pull Request"

on:
  workflow_run:
    workflows: ["Build Pull Request"]
    types:
      - completed

jobs:
  post-build:
    runs-on: ubuntu-latest
    steps:
      - name: info
        run: |
          env
          echo "${{ toJSON(github.event) }}"
